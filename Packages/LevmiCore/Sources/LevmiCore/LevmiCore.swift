/// LevmiCore — framework-freie Domain-Logik von Levmi.
/// Enthält Content-Modell (Gesetze), Progression, Reward-Engine, Kalender/Streak, Save-State.
/// Keine UI, kein SceneKit, kein SwiftUI — zu 100 % unit-testbar.
public enum LevmiCore {
    public static let version = "0.1.0"
}
